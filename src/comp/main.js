import React from 'react';

const Main = ({pageName}) => {
    return (
        <div>
            <main>
                {pageName} page
            </main>
        </div>
    );
}

export default Main;
